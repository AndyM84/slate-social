CREATE TABLE IF NOT EXISTS `Migrations` (
	`ID` INT UNSIGNED NOT NULL AUTO_INCREMENT,
	`FileName` VARCHAR(200) NOT NULL,
	PRIMARY KEY (`ID`)
);

CREATE INDEX `Migration_Files` ON `Migrations` (`FileName`(15));